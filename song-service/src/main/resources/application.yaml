server:
  port: 7083
  error:
    include-stacktrace: never
spring:
  datasource:
    url: jdbc:postgresql://localhost:7003/resources
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
